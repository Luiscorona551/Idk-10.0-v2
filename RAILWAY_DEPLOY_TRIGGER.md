# Railway deployment trigger

Latest IDK 10.0 deployment is on the main branch.

Final desktop fixes: restore the original two-column app layout, prevent overlapping labels by moving each app to the next row, remove the duplicate desktop Apps shortcut, and keep draggable icons aligned with the final grid.
