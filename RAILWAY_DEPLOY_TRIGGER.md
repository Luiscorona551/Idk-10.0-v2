# Railway deployment trigger

Latest IDK 10.0 deployment is on the main branch.

Final desktop fixes: restore the original two-column app layout, move overlapping apps to the next row, and remove the duplicate desktop Apps shortcut so users use the main Apps program instead.
